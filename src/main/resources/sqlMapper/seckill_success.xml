<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.seckill.dao.SeckillSuccessDao">

    <insert id="insertSuccessKilled">
        INSERT INTO seckill_success (goods_id, user_name, create_time)
        VALUES (#{goodsId},#{userName},now())
    </insert>
</mapper>